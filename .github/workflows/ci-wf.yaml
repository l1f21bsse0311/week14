name: ci-workflow
on:
  push:
    - develop
jobs:
  linting-jobs:
    run-on: ubuntu-latest
    steps:
     - name: step 1
     - runs: echo 'yes'
  bulding-jobs:
    needs: 'linting-job'
    runs-on: ubuntu-latest
    steps: 
     - name: step 1
     - runs: echo 'no'

     